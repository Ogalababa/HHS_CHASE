"""
Transport-related domain models.

Rationale: Provide a stable package boundary for transport models (e.g., bus),
so other domain modules can use relative imports without relying on sys.path hacks.
"""

